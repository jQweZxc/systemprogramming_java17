spring:
    profiles:
        active: dev
    application:
        name: BusProject
        version: 1.0.0
---
spring:
    config:
        activate:
            on-profile: dev
    datasource:
        url: jdbc:postgresql://localhost:5432/localhost
        username: postgres
        password: karleon
        driver-class-name: org.postgresql.localhost
        hikari:
            maximum-pool-size: 20
    
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
server:
    port: 8080
---
spring:
    config:
        activate:
            on-profile: prod
server:
    port: 9001