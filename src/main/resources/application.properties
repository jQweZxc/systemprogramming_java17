# File upload configuration
upload.path=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.config.import=optional:secret.properties
spring.application.name=postgres

# JWT Configuration
jwt.secret=66546A555E5A7234753778214123222A472D4B614E645262356B587032733576
jwt.access.duration.minute=5
jwt.access.duration.second=300
jwt.refresh.duration.day=7
jwt.refresh.duration.second=604800
jwt.access.cookie_name=access_token
jwt.refresh.cookie_name=refresh_token

# Database 
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=karleon

# JPA 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.generate-ddl=true
spring.jpa.open-in-view=true

# SQL Data Initialization
spring.sql.init.mode=never
# spring.sql.init.data-locations=classpath:data.sql # временно отключено

# Server
spring.main.banner-mode=off 
spring.output.ansi.enabled=detect

# Logging
logging.level.org.springframework=WARN
logging.level.com.example.firesystem=ERROR
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Security
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

# Performance
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Telegram Bot (заполните своими данными)
telegram.bot.token=YOUR_BOT_TOKEN_HERE
telegram.bot.username=YOUR_BOT_USERNAME
telegram.log.chat.id=YOUR_CHAT_ID

spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

logging.level.org.springframework.security=DEBUG
logging.level.com.example.demo=DEBUG